const INTERNAL_SERVER_ERROR = 'Internal Server Error.';

const CONTACT_SUPPORT = 'Contact Support with following error: ';

const DEPARTMENTS = ['hr', 'engineering', 'operations'];

const UN_AUTHORIZED = 'Unauthorized!';

const INVALID_CREDS = 'Invalid Credentials!';

const ALLOWED_UPDATE_LIST = ['name', 'department'];

const SCHEMA_FIELDS = ['name', 'department'];

module.exports = {
    INTERNAL_SERVER_ERROR,
    CONTACT_SUPPORT,
    DEPARTMENTS,
    UN_AUTHORIZED,
    INVALID_CREDS,
    ALLOWED_UPDATE_LIST,
    SCHEMA_FIELDS
}